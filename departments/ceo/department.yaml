name: ceo
owns: "strategy, key results, the daily brief, work items, reviews, the librarian pass, approval hygiene, the ratchet"
status: active
jobs: ["ceo.daily"]          # ids in docs/jobs.json; the job defines reads, tools, steps, writes, timeout, done_when
servers: []   # defined in docs/mcp-servers.json; a not_built server blocks the job
reads_state: ["preflight", "account-health", "supply-chain"]
harness: claude-code                # the only harness in use; no fallback until one is tested
model: sonnet
tier:
  default: T0                       # anything not listed under classes is T0 and is refused by the runner
  classes: {}                      # Rami edits this map to promote a class; the ceo run proposes, never edits
